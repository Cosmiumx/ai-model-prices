name: 更新 AI 模型价格

on:
  # 定时触发：每天 UTC 时间 00:00（北京时间 08:00）
  schedule:
    - cron: '0 0 * * *'
  # 手动触发
  workflow_dispatch:
  # 推送到 main 分支时也运行（可选）
  push:
    branches:
      - main
    paths:
      - 'fetch_prices.py'
      - 'requirements.txt'

jobs:
  update-prices:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要写权限来提交代码
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 运行价格获取脚本
        run: |
          python fetch_prices.py

      - name: 检查文件是否有变化
        id: verify-changed
        run: |
          if [ -n "$(git status --porcelain model_prices.json)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: 提交更新
        if: steps.verify-changed.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add model_prices.json
          git commit -m "🤖 自动更新: $(date +'%Y-%m-%d %H:%M:%S')"
          git push
